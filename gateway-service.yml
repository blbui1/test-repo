eureka:
    client:
        register-with-eureka: true
        fetch-registry:true
        serviceUrl.defaultZone: http://localhost:8883/eureka
    instance:
        appname:gateway-service
        hostname:localhost
server:
    port:8870