eureka:
    client:
        register-with-eureka: true
        fetch-registry: true
        serviceUrl.defaultZone: http://localhost:8883/eureka
    instance:
        appname: gateway-service
        hostname: localhost
server:
    port: 8870
    
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
      - id: message
        uri: message://configserver-client/rest/message
        predicates:
                - Host=**.configserver-client
    config:
      uri: http://localhost:8884